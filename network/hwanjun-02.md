## 📌 로드밸런서란 무엇인가요? (NET-092)

로드밸런서(Load Balancer)는 클라이언트로부터 들어오는 요청을 여러 서버에 분산시켜 시스템의 **성능, 확장성, 가용성**을 높이는 네트워크 구성 요소이다.

### 핵심 역할

- 트래픽 분산
- 특정 서버 과부하 방지
- 장애 발생 시 서비스 연속성 유지
- 수평 확장(Scale-out) 지원

### 면접용 한 문장

> 로드밸런서는 다수의 서버로 트래픽을 분산해 단일 서버 장애를 방지하고, 전체 시스템의 처리량과 가용성을 향상시키는 장치입니다.

---

## 📌 L4 로드밸런서와 L7 로드밸런서의 차이 (NET-093)

### L4 로드밸런서

- OSI 4계층(Transport Layer)에서 동작
- TCP/UDP 기반으로 IP 주소와 포트 번호만을 기준으로 트래픽을 분산
- 패킷 내용을 해석하지 않음
- 처리 속도가 빠르고 오버헤드가 적음

### L7 로드밸런서

- OSI 7계층(Application Layer)에서 동작
- HTTP/HTTPS 요청의 URL, 헤더, 쿠키 등을 기반으로 트래픽을 분산
- 요청 내용을 이해하고 정교한 라우팅 가능
- 상대적으로 처리 비용이 큼

### 비교 요약

| 구분      | L4        | L7                  |
| --------- | --------- | ------------------- |
| 동작 계층 | 4계층     | 7계층               |
| 판단 기준 | IP, Port  | URL, Header, Cookie |
| 성능      | 빠름      | 상대적으로 느림     |
| 기능      | 단순 분산 | 지능형 라우팅       |

---

## 📌 로드밸런서 알고리즘 (NET-094)

로드밸런서는 트래픽을 분산하기 위해 다양한 알고리즘을 사용합니다.

### 주요 알고리즘

- **Round Robin**: 요청을 순차적으로 분배
- **Least Connections**: 현재 연결 수가 가장 적은 서버로 분배
- **Weighted 방식**: 서버 성능에 따라 가중치를 부여해 분배
- **Hash 기반**: 특정 값(IP 등)을 기준으로 고정 분배

### 면접 포인트

- 단순 분산인지, 부하를 고려한 분산인지 구분해서 설명할 수 있어야 함
- 세션 유지가 필요한 경우 어떤 알고리즘이 적합한지 설명 가능해야 함

---

## 📌 일부 서버 장애 시 요청을 보내지 않게 하려면? (NET-095)

이를 위해 **Health Check(헬스 체크)** 메커니즘을 사용합니다.

### Health Check 개념

- 로드밸런서가 서버의 정상 동작 여부를 주기적으로 확인
- 응답 실패 시 해당 서버를 트래픽 대상에서 자동 제외
- 서버가 정상 상태로 복구되면 다시 트래픽에 포함

### 핵심 효과

- 장애 서버로 요청 전달 방지
- 무중단 서비스 운영 가능
- 자동 장애 감지 및 복구

---

## 📌 DNS를 활용한 로드밸런싱 (NET-096)

로드밸런서 장치 없이도 **DNS를 활용해 트래픽을 분산**할 수 있습니다.

### DNS 기반 로드밸런싱 특징

- 하나의 도메인에 여러 IP 주소 등록
- DNS 서버가 응답 시 IP를 분산 제공
- 구조가 단순하고 비용이 적음

### 한계점

- 실시간 부하 상태 반영 불가
- 장애 감지가 제한적
- TTL 캐시로 인해 장애 IP가 계속 반환될 수 있음
- 정교한 트래픽 제어 어려움

---

## 📌 로드밸런서 vs DNS 로드밸런싱 요약

| 항목        | 로드밸런서        | DNS    |
| ----------- | ----------------- | ------ |
| 실시간 제어 | 가능              | 불가능 |
| 장애 감지   | Health Check 기반 | 제한적 |
| 제어 정밀도 | 높음              | 낮음   |
| 구성 복잡도 | 중                | 낮음   |

---

## 🎯 핵심 정리

- 로드밸런서는 트래픽 분산과 고가용성을 위한 핵심 인프라
- L4는 빠른 분산, L7은 지능형 라우팅에 적합
- 분산 알고리즘 선택은 서비스 특성에 따라 달라짐
- Health Check는 장애 대응의 핵심 메커니즘
- DNS 기반 로드밸런싱은 보조적 수단이며 한계가 명확함

# 추가질문

## Q1. 로드밸런서가 없으면 어떤 문제가 발생하나요?

### ✅ 답변

- 특정 서버에 트래픽이 집중되어 **과부하** 발생
- 단일 서버 장애 시 **서비스 전체 중단(SPOF)**
- 서버를 수평 확장해도 트래픽 분산이 어려움

## Q2. L4와 L7 중 어떤 로드밸런서를 언제 선택해야 하나요?

### ✅ 답변

- **L4 로드밸런서**
  - 빠른 처리량이 중요할 때
  - 단순 TCP/UDP 트래픽 분산
  - 네트워크 레벨 서비스, 게임 서버 등
- **L7 로드밸런서**
  - 요청 내용을 기준으로 분기 필요할 때
  - API Gateway, 웹 서비스
  - 인증, 경로 기반 라우팅 필요 시

> “속도가 중요하면 L4, 제어가 중요하면 L7”

## Q3. L7 로드밸런서는 왜 L4보다 느린가요?

### ✅ 답변

- L7은 HTTP 요청을 **파싱**해야 함
- URL, 헤더, 쿠키 등 **애플리케이션 레벨 정보 분석**
- 추가 연산이 발생 → 오버헤드 증가

## Q4. 로드밸런서 알고리즘은 어떻게 선택하나요?

### ✅ 답변

- 서버 성능이 동일 → **Round Robin**
- 서버 부하 편차 존재 → **Least Connections**
- 서버 스펙 차이 존재 → **Weighted 방식**
- 세션 유지 필요 → **Hash 기반**

## Q5. 세션 유지(Session Stickiness)는 무엇이고 언제 필요한가요?

### ✅ 답변

- 동일 사용자의 요청을 **항상 같은 서버로 전달**하는 방식
- 서버에 세션 상태가 저장된 구조에서 필요

### 주의점

- 특정 서버에 부하 집중 가능
- 서버 장애 시 세션 유실 위험

> 최근에는 Stateless 구조를 선호하고, 세션은 Redis 같은 외부 저장소로 분리하는 것이 일반적

## Q6. Health Check는 왜 필수인가요?

### ✅ 답변

- 서버가 살아있는지 자동으로 판단
- 장애 서버를 **트래픽 대상에서 즉시 제외**
- 복구 시 자동 재편입

## Q7. Health Check가 통과했는데도 장애가 날 수 있지 않나요?

### ✅ 답변

가능합니다.

- 단순 ping/200 OK만 확인하면
- 실제 비즈니스 로직 장애는 감지 못함

### 그래서 실무에서는:

- **의미 있는 헬스 체크 엔드포인트**
- DB, 외부 의존성 상태까지 확인

## Q8. DNS 기반 로드밸런싱과 로드밸런서의 가장 큰 차이는?

### ✅ 답변

- DNS는 **실시간 제어가 불가능**
- TTL 캐시 때문에 장애 반영이 느림
- 헬스 체크 기반 제어 불가

## Q9. 로드밸런서 자체가 장애 나면 어떻게 하나요?

### ✅ 답변

- 로드밸런서도 **이중화 구성** 필요
- 클라우드 환경에서는 관리형 로드밸런서 사용
- 내부적으로 다중 AZ / 다중 인스턴스로 구성됨

## Q10. 로드밸런서에서 HTTPS는 어디서 종료하는 게 좋나요?

### ✅ 답변

- 일반적으로 **로드밸런서에서 SSL 종료**
- 백엔드 서버는 HTTP로 통신
- 인증서 관리, 성능, 운영 편의성 측면에서 유리

> 이를 SSL Termination 또는 TLS Offloading이라고 합니다.
